%h2.title
  Posts
.content
  - @ultimos_posts.each do |post|
    %article
      %h3
        = post.titulo
      %span.date
        = l post.created_at, :format => :short
      %p
        = truncate(post.descripcion, :length => 60)
      = image_tag post.foto(:preview)
